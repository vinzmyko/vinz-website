/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    images: true, // Add this line if you are using the experimental 'images' feature
  },
}

module.exports = nextConfig;
